PORT=8791

# Local demo credentials. Use HTTP Basic Auth: base64(API_KEY:API_SECRET)
API_KEY=local-key
API_SECRET=local-secret

# Optional Web demo password. If empty, the Web page is not password-protected.
WEB_PASSWORD=
WEB_SESSION_COOKIE=pg_web_session
# Set true only when the site is served over HTTPS.
WEB_COOKIE_SECURE=false

# Keep true for local smoke tests without calling a real model.
MOCK_QWEN=true
LLM_TRANSPORT=direct

# Use DashScope OpenAI-compatible endpoint when MOCK_QWEN=false.
QWEN_API_KEY=
QWEN_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
QWEN_MODEL=qwen3.7-max
QWEN_ENABLE_THINKING=false
LLM_MAX_TOKENS=2400

GEMINI_API_KEY=
GEMINI_BASE_URL=https://generativelanguage.googleapis.com/v1beta/openai
# Gemini 3.x are reasoning models; keep effort low for short generations.
GEMINI_REASONING_EFFORT=low

DISPATCHER_BASE_URL=http://47.100.137.178:8080
DISPATCHER_USERNAME=
DISPATCHER_PASSWORD=
DISPATCHER_PROXY=false

# In-memory local queue concurrency. Production should use Redis/DB + worker.
MAX_CONCURRENT_JOBS=2

# ---- Generation pipeline (A/B/C/D) ----
# A = v0 single-stage (baseline)   B = v1 single-stage
# C = v1 two-stage (analyze->generate)   D = v1 three-stage (analyze->golden pool->assemble)
DEFAULT_PIPELINE=C
ANALYZE_MODEL=qwen3.7-max
GENERATE_MODEL=qwen3.7-max
# D golden-line candidate pool (comma-separated). qwen-max = 稳, gemini-3.5-flash = 暖.
GOLDEN_LINE_MODELS=qwen3.7-max,gemini-3.5-flash
ANALYZE_MAX_TOKENS=1800
GOLDEN_MAX_TOKENS=2400

# Meta prompt files
META_PROMPT_VERSION=outbound-v1.0.0
LEGACY_META_PROMPT_PATH=prompts/outbound-v0.md
ANALYZE_META_PROMPT_PATH=prompts/analyze-v1.md
GENERATE_META_PROMPT_PATH=prompts/generate-v1.md
GOLDEN_META_PROMPT_PATH=prompts/golden-lines-v1.md

# Optional Langfuse tracing. Keep disabled unless keys are configured.
LANGFUSE_ENABLED=false
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
LANGFUSE_BASE_URL=https://cloud.langfuse.com
LANGFUSE_ENVIRONMENT=local
LANGFUSE_REDACT_IO=false
